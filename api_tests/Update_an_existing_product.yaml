# Test generated by RoostGPT for test products-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Update an existing product"
    flow:
      - post:
          url: "/products/1"
          json:
            name: "Updated Product"
            description: "This is an updated test product."
            price: 15.0
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
          expect:
            - statusCode: 200
            - equals: 
                - "{{ response.body.name }}"
                - "Updated Product"
            - equals: 
                - "{{ response.body.description }}"
                - "This is an updated test product."
            - equals: 
                - "{{ response.body.price }}"
                - 15.0
          # The product with ID 1 should be updated with the provided details
