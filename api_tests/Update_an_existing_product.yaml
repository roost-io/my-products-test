# Test generated by RoostGPT for test products-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Update an existing product"
    flow:
      - post:
          url: "/products/1"
          json:
            name: "Updated Product"
            description: "This is an updated test product"
            price: 15.0
          headers:
            content-type: "application/json"
          capture:
            - json: "$"
              as: "updateResponse"
          expect:
            - statusCode: 200
            - hasProperty: updateResponse
            # Assumption made here about response format without any reference
            - equals: 
                - "{{ updateResponse.data.name }}"
                - "UpdatedProduct"
            - equals: 
                - "{{ updateResponse.data.description }}"
                - "This is an updated test product"
            - equals: 
                - "{{ updateResponse.data.price }}"
                - 15.0
