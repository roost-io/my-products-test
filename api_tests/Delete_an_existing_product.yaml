# Test generated by RoostGPT for test products-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  processor: "./processors.js"
  variables:
    productId: [1]

scenarios:
  - name: "Delete an existing product"
    flow:
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200
          # Verify the product with ID 1 exists
      - delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200
            - body: 'Product with ID 1 should no longer exist'
          # Test delete an existing product
