# Test generated by RoostGPT for test products-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Delete an existing product"
    # The flow describes the steps taken by the scenario
    flow:
      - get:
          url: "/products/1"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
      - delete:
          url: "/products/1"
          expect:
            - statusCode: 200
      - get:
          url: "/products/1"
          expect:
            - statusCode: 404
