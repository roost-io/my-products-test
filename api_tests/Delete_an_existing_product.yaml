# Test generated by RoostGPT for test products-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Delete an existing product"
    flow:
      - log: "Deleting a product with ID 1"
      - delete:
          url: "/products/1"
          capture:
            - json: "$.statusCode"
              as: "status"
          expect:
            - statusCode: 200
      - think: 1
      - get:
          url: "/products/1"
          expect:
            - statusCode: 404
